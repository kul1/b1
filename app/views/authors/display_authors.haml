
%table{:id=>"pending-table", :width=>"100%"}
  %tr{:style=>"color:white; background-color:#97BF60;"}
    %th เรื่อง
    %th วันที่
    %th ลบ
  - @authors.each do |author|
    %tr
      %td= link_to author.name, :controller=>"authors", :action=>"show_author", :id=>author.id
      %td= date_thai author.created_at
      %td(align='center')= link_to image_tag('delete.png', style:'border:none; float:none;'), "#", :onclick=>"if (confirm('กรุณายืนยัน')) {location.hash='/authors/rm/#{author.id}';}"